meta:
  environment: jvm
  toolchain:
    name: maven
    version: 11
build:
  artifacts:
    target/*.jar: /
run:
  jarName: /app/target/Starter-1.0-SNAPSHOT.jar
  persistenceMount: /data
  containerPort: 80